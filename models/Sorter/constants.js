const SEPARTOR_KEY = ':';
const EMAIL_PASS_REGEX = /^[a-z0-9_.+-]+@[a-z0-9-]+\.[a-z0-9-.]+:[a-z0-9-.]+/im;
const SYMBOL_AT = '@';

module.exports = {
  SEPARTOR_KEY,
  EMAIL_PASS_REGEX,
  SYMBOL_AT,
};
